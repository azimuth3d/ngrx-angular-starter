import TestBed from '@angular/core/testing';
import * as fromActions from '../actions/githunt.actions';
import * as fromReducers from './githunt.reducer';

describe('All githunt module reducers perform correctly', () => {
  beforeEach(() => {});
  test('githunt search repo reducer must working properly', () => {
    expect(true).toBeTruthy();
  });
});
